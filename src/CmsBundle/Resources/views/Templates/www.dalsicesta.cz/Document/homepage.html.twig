   {{ cms_region(document, 'global.menu') }}

   {{ cms_region(document, 'global.head') }}

    <!-- End Header Logo & Navigation -->

    {#{ cms_region(document, 'global.slider') }#}

    <div class="cleaner"></div>

    {{ cms_region(document, 'page.content') }}

    {#{ cms_region(document, 'page.activities') }}

    {{ cms_region(document, 'global.news') }}

    {{ cms_region(document, 'page.content2') }}

    {{ cms_region(document, 'page.counter') }}

    {{ cms_region(document, 'page.content3') }#}

    {{ cms_region(document, 'global.foot') }}