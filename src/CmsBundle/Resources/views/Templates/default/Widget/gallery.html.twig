 {#% if  images[0] is defined %}

   {% if parameters.link %}
   <a href="{{ parameters.link }}">
   {% endif %}
      <img class="responsive-image{{ (parameters.classImage ? ' ' ~  parameters.classImage ~ ''  : '') | raw  }}" src="{{ asset(images[0].src) | imagine_filter('cms_image_thumb') }}" alt="{{ parameters.alt }}" />
   {% if parameters.link %}
      </a>
   {% endif %}

   {% if parameters.gallery %}
      <a class="{{ parameters.gallery }}" href="{{ asset(images[0].src) | imagine_filter('cms_image_large') }}">
   {% endif %}
     <img class="responsive-image{{ (parameters.classImage ? ' ' ~  parameters.classImage ~ ''  : '') | raw  }}" src="{{ asset(images[0].src) | imagine_filter('cms_image_thumb') }}" alt="{{ parameters.alt }}" />
   {% if parameters.gallery %}
      </a>
   {% endif %}

   {% if parameters.annotation %}
   <p class="center">{{ parameters.annotation }}</p>
   {% endif %}

 {% endif %#}

 {% if parameters.gallery %}
    <div class="gallery row">
    {% for image in parameters.gallery.images %}
      <div class="image-holder {{ parameters.width }}">
        <a class="image-link" data-fancybox="gallery-{{ parameters.gallery.id }}" href="{{ asset(image.path) | imagine_filter('cms_image_large') }}">
          <img width="100%" class="image responsive-image{{ (parameters.classImage ? ' ' ~  parameters.classImage ~ ''  : '') | raw  }}" src="{{ asset(image.path) | imagine_filter('cms_image_thumb') }}" alt="{{ parameters.gallery.name }}" />
        </a>
      </div>
    {% endfor %}
    </div>
 {% else %}
  Galerie nebyla vybrána nebo neexistuje
 {% endif %}