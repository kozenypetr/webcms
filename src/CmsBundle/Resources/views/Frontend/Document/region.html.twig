<div class="container region region-{{ name }}" data-region="{{ name }}" data-type="{{ type }}">
    <div class="row">
    {% for widget in widgets %}
        {{ cms_widget(widget) }}
    {% endfor %}
    </div>
</div>